services:
  minecraft:
    image: itzg/minecraft-server:java21
    container_name: rainbowranks-test
    restart: unless-stopped
    tty: true
    stdin_open: true
    ports:
      - "25565:25565"
    environment:
      EULA: "TRUE"
      TYPE: "PAPER"
      VERSION: "1.21.8"
      MEMORY: "G"
      MOTD: "Vexssa Test"
      ENABLE_RCON: "false"
      ONLINE_MODE: "true"
    volumes:
      - ./test-server:/data
      - ./build/libs:/plugins:ro
